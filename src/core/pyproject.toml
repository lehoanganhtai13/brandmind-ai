[project]
name = "core"
version = "0.1.0"
requires-python = ">=3.10,<3.11"
dependencies = [
    "shared",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.setuptools.packages.find]
where = ["src"]